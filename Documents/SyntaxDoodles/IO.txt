Console
	Output
		print ""

	Input
		x: read

		x: read \x => int?		// or alternately readIf, readNext, readAll

	Note: io.print = print, io.read = read		// or have io.std.out => io.print = io.writer io.std.out
	Note: print and read are mutable, io.print and io.read are not

File/Custom Console				// Creation of objects, etc.
	x: io.reader "file"/"console object", [accessors]
	x: io.writer "file"/"console object", [accessors]

Logging
	io.log ""