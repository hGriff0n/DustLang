These tests are designed to ensure that
	1) The parser and interpreter work correctly
	2) The parser produces correct errors on syntactically wrong code
	3) The interpreter produces correct errors on semantically wrong code
	4) The parser produces warnings on possibly harmful code (???)
	5) That error handling is performed correctly (???)

Included in this filter are
	1) Syntactically ambiguous files from the Parser filter
	2) Syntactically correct files from the Parser filter
	3) Semantically correct files from the Interpreter filter
	4) Semantically ambiguous files
	5) Semantically wrong files

// More ???